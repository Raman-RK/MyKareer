events_xpath = '//*[contains(@class, "text--black nav-link cursor--pointer") and contains(., "Events")]'
opportunities_xpath = "//*[contains(@class, 'text--black nav-link cursor--pointer') and contains(., 'Opportunities')]"
faq_xpath = "//*[contains(@class, 'text--black nav-link cursor--pointer') and contains(., 'FAQ')]"
plans_xpath = "//*[contains(@class, 'text--black nav-link cursor--pointer') and contains(., 'Plans')]"
mode_css = '[class="text--grey nav-link cursor--pointer"]'
signup_css = '.d-flex.align-items-center.gap-3 #signup'  # first element
login_id = "#login"  # first element
cover_image_css = '[class="w-100 overflow-hidden"]'
open_events_img_css = '[class="w-100 overflow-hidden object-fit-cover mb-3"]'

# contact us
name_xpath = '[//*[contains(@class, "form-label font--14   ") and contains(text(),  "Your full name*")]]'
email_xpath = '[//*[contains(@class, "form-label font--14   ") and contains(text(),  "Your email*")]]'
phone_xpath = '[//*[contains(@class, "form-label font--14   ") and contains(text(),  "Your phone*")]]'
headline_xpath = '[//*[contains(@class, "form-label font--14   ") and contains(text(),  "Headline")]]'
confirm_xpath = '[//button[contains(text(), "Confirm")]]'